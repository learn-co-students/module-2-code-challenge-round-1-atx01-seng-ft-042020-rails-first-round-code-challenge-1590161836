<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%= link_to power.name, power_path(power) %>
      <br>
      Description: <%= power.description %>
      <br>
      This many people have this power: <% HeroinePower.find_by(power_id: power.id) %>
    </li>
    <br>
  <% end %>
</ul>
