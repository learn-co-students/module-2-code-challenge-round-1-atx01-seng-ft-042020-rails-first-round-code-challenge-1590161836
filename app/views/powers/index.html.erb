<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%= power.name %>
      <br>
      Description: <%= power.description %>
      <br>
      <%= pluralize(power.heroines.count, "heroine") %> with this power!
      <br> 
    </li>
    <br>
  <% end %>
</ul>
