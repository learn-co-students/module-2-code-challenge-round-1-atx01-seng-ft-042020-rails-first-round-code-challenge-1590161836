<h1> Edit Power </h1>

<%= form_for @power do |f| %>
    <%= f.text_field :name %>
    <br>
    <%= f.text_area :description %>
    <br>
    <%= f.collection_select :heroine_ids, @heroines, :id, :name, {},{multiple: true } %>
    <br>
    <%= f.submit %>
<% end %>